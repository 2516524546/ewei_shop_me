#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_csslint@1.0.5@csslint/dist/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_csslint@1.0.5@csslint/dist/cli.js" "$@"
  ret=$?
fi
exit $ret
