这个目录下是 C# 的 demo，请使用 msvs 2012 打开 csdemo.sln。

C# 使用非托管的模式调用 dll。

public.pem 和 ec_key.pem 是公私钥，用于 demo 的使用。

在使用 demo 时，请注意公私钥的路径，请注意工程配置使用的 32 位还是 64位，进而加载相应版本的 dll。

demo 在使用时，只需要修改公私钥的路径，和 dll 加载的路径即可运行使用，加载 dll 
时请关注工程配置是 32 位还是 64，否则会出现载入的 dll 格式不正确的异常。

Any CPU 平台项目，默认需要调用 32 位 dll。